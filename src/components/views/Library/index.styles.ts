import { css } from '@emotion/react';

const styles = {
  mainContainer: css({
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
  }),
  notification: css({
    flexDirection: 'column',
  }),
};

export default styles;