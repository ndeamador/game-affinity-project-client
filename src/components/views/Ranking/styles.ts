import { css } from '@emotion/react';

const styles = {
  containerStyle: css({
    display: 'flex',
    flexDirection: 'column',
  }),
  noRatingsNotification: css({
    flexDirection: 'column',
  }),
};

export default styles;